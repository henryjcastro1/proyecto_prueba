import 'package:flutter/material.dart';

class ResenaFotografica extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Reseña Fotográfica'),
      ),
      body: Center(
        child: Text('Contenido de la reseña fotográfica'),
      ),
    );
  }
}
